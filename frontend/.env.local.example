# Base URL of the Mental Coach API (no trailing slash).
# Local: run the API (e.g. uvicorn) and set this to http://localhost:8000
# Vercel: set to your deployed API URL.
NEXT_PUBLIC_API_URL=http://localhost:8000
